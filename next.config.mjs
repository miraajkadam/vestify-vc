// /** @type {import('next').NextConfig} */

// const nextConfig = {
//   // Enabling React Strict Mode for development
//   reactStrictMode: true,
//   // Add custom Webpack configuration
//   webpack(config, { dev }) {
//     if (dev) {
//       config.watchOptions = {
//         aggregateTimeout: 300, // Delay before rebuilding after changes
//         poll: 1000, // Enable polling for file changes (useful in Docker)
//       };
//     }
//     return config;
//   },
// };

// export default nextConfig;

// /** @type {import('next').NextConfig} */

// const nextConfig = {
//   // Enabling React Strict Mode for development
//   reactStrictMode: true,

//   // Add custom Webpack configuration
//   webpack(config, { dev }) {
//     if (dev) {
//       config.watchOptions = {
//         aggregateTimeout: 300, // Delay before rebuilding after changes
//         poll: 1000, // Enable polling for file changes (useful in Docker)
//       };
//     }

//     return config;
//   },
// };

// export default nextConfig;

// import type { NextConfig } from 'next';

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

// "@walletconnect/client": "^1.8.0",
